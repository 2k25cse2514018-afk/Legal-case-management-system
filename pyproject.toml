[project]
name = "legal-case-management"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "flask-login",
    "flask-sqlalchemy",
    "python-dotenv",
    "google-generativeai"
]

[tool.vercel]
entrypoint = "legal_case_management.app:app"